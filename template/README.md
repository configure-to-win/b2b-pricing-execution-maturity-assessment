# Workbook location

Place the production workbook here as:

`b2b-pricing-execution-maturity-assessment.xlsx`

The workbook is deliberately not included in this preparation package. Add the validated release workbook before publishing the repository or creating release `v1.0.0`.
